const page=(location.pathname.split('/').pop()||'index.html').toLowerCase();
const labels=[['products.html','Products'],['apps.html','Apps and Services'],['learn.html','Learn'],['business.html','For Business'],['developers.html','For Developers'],['support.html','Support']];
const promo=document.querySelector('.announcement');
if(promo){promo.className='ledger-topbar';promo.innerHTML='<span>Meet Ledger Nano™ Gen5, the most playful signer yet</span><a href="products.html">Discover now <b>→</b></a>'}
const header=document.querySelector('.site-header');
if(header){header.className='ledger-header';header.innerHTML=`<div class="ledger-nav"><a class="ledger-logo" href="index.html"><span>LEDGER</span></a><button class="ledger-menu" type="button" aria-label="Open navigation" aria-expanded="false"><i></i><i></i></button><nav class="ledger-links">${labels.map(([href,label])=>`<a href="${href}"${page===href?' aria-current="page"':''}>${label}</a>`).join('')}</nav><div class="ledger-tools"><button type="button" aria-label="Choose language">EN⌄</button><a href="products.html" aria-label="Shopping cart" class="ledger-cart"><span></span></a></div></div>`;const menu=header.querySelector('.ledger-menu');const links=header.querySelector('.ledger-links');menu.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('open')))}
if(page==='support.html'){const list=document.querySelector('#safety .list');if(list)list.insertAdjacentHTML('beforeend','<a class="list-item" href="phishing-campaigns-status.html"><b>04</b><h3>Check current phishing campaigns</h3><p>Review active scam patterns, warning signs and reporting steps in the Ledger security alert center.</p></a>')}
